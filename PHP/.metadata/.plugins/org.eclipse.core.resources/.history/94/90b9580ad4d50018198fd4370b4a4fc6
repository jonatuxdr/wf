<?php

require_once __DIR__.'/DimensionalMath/Distance/DistanceCalculation.php';
require_once __DIR__.'/DimensionalMath/Vector/AngleCalculation.php';

use function DimensionalMath\Distance\subSquare as sub;
use function DimensionalMath\Distance\threeDimensionDistance as three;
use function DimensionalMath\Vector\getVectorAngle as vect;

$sub = sub(10, 7);
$distance = three(
    [1, 1, 1],
    [2, 2, 2]
);

$angle = vect([1, 6], [3, 12]);